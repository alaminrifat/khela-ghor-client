import React from 'react';
import setTitle from '../../hook/setTitle';

const Blog = () => {
    setTitle('Blog');

    return (
        <div>
            
        </div>
    );
};

export default Blog;